
import type { StylePreset } from '../types/audio';
import { DUBSTEP_PRESETS } from './dubstep'
import { LOFI_PRESETS } from './lo-fi'

export const STYLE_PRESETS: readonly StylePreset[] = [
    DUBSTEP_PRESETS,
    LOFI_PRESETS,
]
